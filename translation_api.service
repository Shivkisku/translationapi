# translation_api.service

[Unit]
Description=Gunicorn instance to serve translation API
After=network.target

[Service]
User=your_user
Group=www-data
WorkingDirectory=/Documents/python
ExecStart=/usr/local/bin/gunicorn -c config/gunicorn_config.py app:app

[Install]
WantedBy=multi-user.target
